# 🗑️ Очистка базы данных Stock Quotes

## Описание

Инструменты для полной очистки базы данных от всех записей для развертывания с чистого листа.

---

## 🚀 Быстрый старт

### Windows (рекомендуется)

Запустите BAT-файл:

```cmd
clear_database.bat
```

Появится интерактивное меню с выбором действий.

### Командная строка

```bash
# Показать информацию о БД
python clear_database.py --info

# Очистить все таблицы (рекомендуется)
python clear_database.py --clear

# Удалить файл БД полностью
python clear_database.py --delete

# Без подтверждения (для автоматизации)
python clear_database.py --clear --force
```

---

## 📋 Варианты очистки

### Вариант 1: Очистка таблиц (рекомендуется)

```bash
python clear_database.py --clear
```

**Преимущества:**
- ✅ Удаляет все записи из всех таблиц
- ✅ Сохраняет структуру БД (таблицы, индексы)
- ✅ Сбрасывает счетчики автоинкремента
- ✅ Быстрее работает при следующем запуске
- ✅ Не требует пересоздания схемы

**Что очищается:**
- `accuracy_history` - история точности прогнозов
- `consensus` - консенсус моделей
- `analysis_results` - результаты анализа
- `stocks` - котировки акций
- `companies` - информация о компаниях

### Вариант 2: Удаление файла БД

```bash
python clear_database.py --delete
```

**Особенности:**
- ⚠️ Полностью удаляет файл базы данных
- ⚠️ БД будет пересоздана при следующем запуске
- ⚠️ Требует времени на создание структуры

**Когда использовать:**
- Если файл БД поврежден
- Если нужно гарантированно начать с нуля
- Если размер файла слишком большой

---

## 🔍 Проверка состояния БД

```bash
python clear_database.py --info
```

**Показывает:**
- Путь к файлу БД
- Размер файла
- Количество записей в каждой таблице
- Общее количество записей

**Пример вывода:**

```
═══════════════════════════════════════════════════════════════
📊 ИНФОРМАЦИЯ О БАЗЕ ДАННЫХ
═══════════════════════════════════════════════════════════════
Файл: data/stock_analysis.db
Размер: 245.50 KB

  Компании                 :     50 записей
  Котировки                :    150 записей
  Результаты анализа       :    450 записей
  Консенсус                :    150 записей
  История точности         :    300 записей

  ИТОГО                    :   1100 записей
═══════════════════════════════════════════════════════════════
```

---

## 💡 Примеры использования

### Пример 1: Проверка и очистка

```bash
# 1. Сначала посмотреть что в БД
python clear_database.py --info

# 2. Очистить если нужно
python clear_database.py --clear
```

### Пример 2: Быстрая очистка без подтверждения

```bash
python clear_database.py --clear --force
```

Полезно для скриптов автоматизации.

### Пример 3: Работа с другой БД

```bash
python clear_database.py --info --db-path "backup/old_database.db"
python clear_database.py --clear --db-path "backup/old_database.db"
```

---

## ⚠️ Важные замечания

### Безопасность

1. **Запрашивается подтверждение**
   - По умолчанию скрипт требует подтверждения (yes/NO)
   - Используйте `--force` только если уверены

2. **Необратимое действие**
   - Восстановить удаленные данные невозможно
   - Сделайте резервную копию если нужно:
     ```bash
     copy data\stock_analysis.db data\stock_analysis_backup.db
     ```

3. **Закройте приложение**
   - Перед очисткой закройте все программы, использующие БД
   - Особенно веб-интерфейс (`start_web.bat`)

### Что происходит после очистки

После очистки БД:
- ✅ Все записи удалены
- ✅ Структура БД готова к работе
- ✅ Можно сразу запускать анализ
- ✅ При запуске данные загрузятся из Excel

---

## 🔧 Автоматизация

### Скрипт для полного пересоздания

Создайте `reset_project.bat`:

```batch
@echo off
echo Очистка базы данных...
python clear_database.py --clear --force

echo Удаление экспортов...
del /q output\exports\*.xlsx 2>nul

echo Удаление логов...
del /q logs\*.log 2>nul

echo ✅ Проект готов к развертыванию с чистого листа!
pause
```

### Добавление в scheduler

Можно добавить очистку старых данных в планировщик:

```python
# В scheduler.py
def cleanup_old_data():
    """Очистка данных старше 90 дней"""
    from clear_database import clear_old_records
    clear_old_records(days=90)
```

---

## 📊 Структура таблиц

Информация о таблицах в БД:

### companies
- Основная информация о компаниях
- Связь: `company_id` во всех остальных таблицах

### stocks
- Котировки акций по датам
- Связь с `companies` через `company_id`
- Уникальность: `company_id + analysis_date`

### analysis_results
- Результаты анализа моделями
- Связь с `stocks` через `stock_id`
- Множественные записи (по модели на каждую акцию)

### consensus
- Итоговый консенсус всех моделей
- Связь с `stocks` через `stock_id`

### accuracy_history
- История точности прогнозов
- Для отслеживания качества моделей

---

## 🆘 Решение проблем

### База данных заблокирована

```
Error: database is locked
```

**Решение:**
1. Закройте веб-интерфейс
2. Завершите все Python процессы
3. Повторите попытку

### Файл не найден

```
⚠️  База данных не существует
```

**Это нормально:**
- БД еще не создана
- Будет создана при первом запуске
- Ничего очищать не нужно

### Ошибка доступа

```
PermissionError: [WinError 32]
```

**Решение:**
1. Закройте все программы, использующие БД
2. Проверьте права доступа к папке `data/`
3. Запустите от имени администратора

---

## 🔗 Связанные инструменты

После очистки БД используйте:

```bash
# Проверить котировки
python check_stocks.py

# Загрузить тестовые данные
python create_initial_data.py

# Запустить анализ
start.bat

# Открыть веб-интерфейс
start_web.bat
```

---

## 📝 Логирование

Все операции логируются в:
- **Консоль** - для пользователя
- **Лог-файл** - `clear_database.log` (если включено)

Формат записей:

```
2026-02-04 10:30:15 - INFO - Очистка всех таблиц в БД
2026-02-04 10:30:15 - INFO - Очищена таблица: companies
2026-02-04 10:30:15 - INFO - База данных успешно очищена
```

---

## 💬 Часто задаваемые вопросы

**Q: Что лучше - очистить таблицы или удалить файл?**  
A: Рекомендуется очищать таблицы (`--clear`). Это быстрее и сохраняет структуру БД.

**Q: Можно ли восстановить данные после очистки?**  
A: Нет, операция необратима. Сделайте резервную копию перед очисткой.

**Q: Нужно ли очищать БД перед каждым анализом?**  
A: Нет. БД накапливает историю. Очищайте только для полного пересоздания.

**Q: Что будет с Excel файлом?**  
A: Excel файл не затрагивается. Очищается только база данных.

**Q: Сколько места освободится?**  
A: Зависит от количества данных. Проверьте через `--info` перед очисткой.

---

## ✅ Чек-лист перед очисткой

- [ ] Закрыт веб-интерфейс
- [ ] Закрыты все программы, использующие БД
- [ ] Сделана резервная копия (если нужно)
- [ ] Проверено состояние БД (`--info`)
- [ ] Понятны последствия очистки

---

**Готово!** Теперь можно развернуть проект с чистого листа 🎉
