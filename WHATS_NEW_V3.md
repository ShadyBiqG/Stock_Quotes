# 🎉 Что нового в версии 3.0

## 📋 Основные изменения

### ⚙️ Новая структура конфигурации

**Было (v2.x):**
```
config.yaml  # Все настройки + API ключи в одном файле
```

**Стало (v3.0):**
```
config/
├── api_keys.yaml       # 🔑 Только API ключи
├── llm_config.yaml     # ⚙️ Все настройки проекта
└── companies.json      # 🏢 Список компаний
```

**Преимущества:**
- ✅ Безопасность: API ключи в отдельном файле
- ✅ Удобство: все настройки в одном месте (`llm_config.yaml`)
- ✅ Гибкость: JSON для компаний, YAML для настроек
- ✅ Не попадет в Git: все файлы в `.gitignore`

---

### 🔑 Где находятся настройки

#### 1. API ключи → `config/api_keys.yaml`
```yaml
openrouter_api_key: "sk-or-v1-ваш-ключ"
alphavantage_api_key: ""  # опционально
```

#### 2. ВСЕ настройки проекта → `config/llm_config.yaml`
- 🤖 Модели LLM (`models`)
- 📝 Промпты (`system_prompt`, `prompt_template`)
- 🗄️ База данных (`database`)
- 🔄 Обновление котировок (`price_updates`)
- 📤 Экспорт (`export`)
- 🏢 Информация о компаниях (`company_info`)
- ⏰ Планировщик (`scheduler`)
- 🌐 Веб-приложение (`web_app`)
- 📋 Логирование (`logging`)

#### 3. Список компаний → `config/companies.json`
```json
{
  "companies": [
    {"ticker": "AAPL"},
    {"ticker": "MSFT"}
  ]
}
```

---

### 🏢 Автоматическое управление компаниями

**Раньше (v2.x):**
- ❌ Ручное редактирование Excel файла
- ❌ Ручное добавление цен
- ❌ Нет синхронизации

**Теперь (v3.0):**
- ✅ Веб-интерфейс для добавления/удаления компаний
- ✅ Автоматическое получение цен через Yahoo Finance
- ✅ Автоматическое получение описаний через LLM
- ✅ Автосинхронизация с `companies.json`

**Как добавить компанию:**
1. Откройте веб-интерфейс: `streamlit run app.py`
2. Перейдите в "⚙️ Настройки" → "Управление компаниями"
3. Введите тикер (например, `NVDA`)
4. Нажмите "Добавить"
5. Готово! Все данные получатся автоматически

---

### 🔄 Стратегии обновления котировок

Новая секция `price_updates` в `config/llm_config.yaml`:

```yaml
price_updates:
  strategy: "daily"  # always | daily | cache_only
  min_update_interval_minutes: 15
```

**Стратегии:**
- `always` — всегда загружать свежие (медленно, актуально)
- `daily` — раз в день (**рекомендуется** для быстрых повторных запусков)
- `cache_only` — только из БД (быстро, но устаревает)

---

### 🇷🇺 Русский язык во всех ответах

**Обновлены промпты:**
- ✅ Описания компаний на русском
- ✅ Анализ акций на русском
- ✅ Развернутые ответы (4-6 предложений)
- ✅ Конкретные цифры и уровни

**Было:**
```
AMD is a multinational semiconductor company...
```

**Стало:**
```
Эдванс Майкро Дивайсес - американская компания, 
специализирующаяся на разработке полупроводников...
```

**Промпты увеличены:**
- Было: `max_tokens: 1000`
- Стало: `max_tokens: 1500` (+50%)

---

### 📚 Новая документация

**Созданные файлы:**
- `QUICK_START_V3.md` — быстрый старт
- `CHANGELOG_CONFIG.md` — изменения в конфигурации
- `CHANGELOG_PROMPTS.md` — изменения промптов
- `SUMMARY_CHANGES.md` — полная сводка изменений
- `WHATS_NEW_V3.md` — этот файл
- `test_config_loading.py` — тест конфигурации

**Обновлены:**
- `README.md` — добавлен раздел "Где находятся все настройки"
- `docs/ru/МИГРАЦИЯ_V3.md` — подробная инструкция по миграции

---

## 🚀 Как начать использовать v3.0

### Новый пользователь

```bash
# 1. Скопируйте примеры
copy config\api_keys.example.yaml config\api_keys.yaml
copy config\llm_config.example.yaml config\llm_config.yaml
copy config\companies.example.json config\companies.json

# 2. Добавьте API ключ в config/api_keys.yaml

# 3. Запустите
streamlit run app.py
```

### Миграция с v2.x

```bash
# Автоматическая миграция
python scripts/migrate_to_v3.py
```

Скрипт автоматически:
- ✅ Создаст новую структуру конфигурации
- ✅ Перенесет API ключ
- ✅ Экспортирует компании из БД в JSON
- ✅ Сохранит резервную копию старого `config.yaml`

---

## 🔍 Проверка работоспособности

```bash
# Тест конфигурации
python test_config_loading.py

# Тест котировок
python test_price_loading.py

# Проверка БД
python check_current_db_data.py
```

Все тесты должны показать:
```
✅ ВСЕ ПРОВЕРКИ ПРОЙДЕНЫ!
```

---

## 📊 Сравнение версий

| Функция | v2.x | v3.0 |
|---------|------|------|
| Конфигурация | 1 файл | 3 файла (разделение) |
| API ключи | В общем файле | Отдельный файл ✅ |
| Список компаний | Excel | JSON ✅ |
| Получение цен | Вручную | Автоматически ✅ |
| Описания компаний | Вручную | Автоматически ✅ |
| Веб-управление | Нет | Есть ✅ |
| Русский язык | Частично | Полностью ✅ |
| Развернутые ответы | 1-2 предложения | 4-6 предложений ✅ |
| Безопасность | Риск утечки ключей | Защищено ✅ |
| Стратегии обновления | Нет | 3 стратегии ✅ |

---

## 💡 Рекомендуемые настройки

### Для начинающих

```yaml
# config/llm_config.yaml
models:
  - name: "GPT-3.5 Turbo"
    id: "openai/gpt-3.5-turbo"
    temperature: 0.3
    max_tokens: 1000  # Экономия токенов

price_updates:
  strategy: "daily"  # Быстро и актуально
```

### Для продвинутых

```yaml
# config/llm_config.yaml
models:
  - name: "GPT-4 Turbo"
    id: "openai/gpt-4-turbo"
    temperature: 0.3
    max_tokens: 1500
    
  - name: "Claude 3.5 Sonnet"
    id: "anthropic/claude-3.5-sonnet"
    temperature: 0.3
    max_tokens: 1500
    
  - name: "Gemini Pro 1.5"
    id: "google/gemini-pro-1.5"
    temperature: 0.3
    max_tokens: 1500

price_updates:
  strategy: "always"  # Всегда свежие данные
```

---

## ❓ Частые вопросы

### Как вернуться к v2.x?

Старый `config.yaml` поддерживается для обратной совместимости. Просто удалите папку `config/` и используйте `config.yaml`.

### Где хранятся данные?

- **Конфигурация:** `config/` (не в Git)
- **База данных:** `data/stocks.db`
- **Кэш:** `data/cache/`
- **Экспорт:** `output/exports/`
- **Логи:** `logs/`

### Как обновить промпты?

Отредактируйте `config/llm_config.yaml`:
- `system_prompt` — роль и правила для LLM
- `prompt_template` — шаблон запроса для анализа

### Сколько стоит использование?

10 компаний × 3 модели ≈ **$0.15-0.30** за анализ

Используйте `daily` стратегию для экономии!

---

## 🎯 Что дальше?

1. ✅ Ознакомьтесь с новой структурой конфигурации
2. ✅ Мигрируйте на v3.0 через `scripts/migrate_to_v3.py`
3. ✅ Попробуйте добавить компанию через веб-интерфейс
4. ✅ Настройте автоматический запуск по расписанию
5. ✅ Изучите развернутые анализы с конкретными цифрами

---

**Версия:** 3.0.0  
**Дата:** 2026-02-04  
**Статус:** ✅ Стабильная
