# Руководство по миграции на новую версию

## Изменения в версии 2.0

### Основные изменения

1. **Excel файл теперь содержит только список тикеров**
   - Удалены колонки: `Price`, `Change`, `Volume`, `Info`
   - Осталась только колонка: `Ticker`
   - Все данные о котировках теперь хранятся в базе данных

2. **Улучшено удаление компаний**
   - Теперь кнопка удаления работает корректно
   - Каскадное удаление всех связанных записей
   - Удаляются котировки, анализы, консенсус и история

### Почему были сделаны изменения?

- **Упрощение структуры**: Excel файл теперь используется только для указания списка компаний для анализа
- **Централизация данных**: Все котировки хранятся в БД, что обеспечивает историю изменений
- **Гибкость**: Можно легко добавлять/удалять компании через веб-интерфейс

### Миграция существующих данных

#### Автоматическая миграция

Запустите скрипт обновления Excel файла:

```bash
python update_excel.py
```

Скрипт:
- Создаст резервную копию (`Stock quotes_backup.xlsx`)
- Удалит ненужные колонки
- Оставит только список тикеров

#### Ручная миграция

Если нужно создать новый файл вручную:

1. Откройте Excel
2. Создайте файл с одной колонкой `Ticker`
3. Заполните тикеры компаний (по одному на строку):
   ```
   Ticker
   NVDA
   AVGO
   TSM
   AAPL
   ```
4. Сохраните как `Stock quotes.xlsx`

### Как работает новая система?

1. **Первый запуск**: 
   - Загружаются тикеры из Excel
   - Для компаний без данных в БД используются начальные значения (цена = 100)
   - После анализа котировки сохраняются в БД

2. **Последующие запуски**:
   - Загружаются тикеры из Excel
   - Котировки берутся из БД (последние сохраненные)
   - После нового анализа данные обновляются в БД

3. **Добавление новой компании**:
   - Добавьте тикер в Excel файл, ИЛИ
   - Используйте веб-интерфейс: Настройки → Управление компаниями → Добавить

4. **Удаление компании**:
   - Через веб-интерфейс: Настройки → Управление компаниями → кнопка 🗑️
   - Удаляются все связанные данные автоматически

### Примеры использования

#### Добавление компании через веб-интерфейс

1. Откройте веб-приложение (`start_web.bat`)
2. Перейдите в "⚙️ Настройки"
3. Вкладка "🔧 Конфигурация"
4. Раздел "🏢 Управление компаниями"
5. Введите тикер (например: `AAPL`)
6. Нажмите "✅ Добавить"
7. Информация о компании будет получена автоматически

#### Удаление компании

1. В том же разделе "Управление компаниями"
2. Найдите компанию в списке
3. Нажмите кнопку 🗑️
4. Все данные (котировки, анализы, история) будут удалены

#### Обновление котировок

Котировки обновляются автоматически при каждом запуске анализа.
Система сохраняет историю всех котировок с датами.

### Проверка миграции

После миграции проверьте:

1. ✅ Excel файл содержит только колонку `Ticker`
2. ✅ Веб-интерфейс открывается (`start_web.bat`)
3. ✅ В настройках видны все компании
4. ✅ Кнопка удаления работает
5. ✅ Можно добавлять новые компании

### Откат изменений

Если что-то пошло не так:

1. Восстановите резервную копию Excel:
   ```
   Stock quotes_backup.xlsx → Stock quotes.xlsx
   ```

2. Восстановите старую версию кода из git:
   ```bash
   git checkout HEAD~1
   ```

### Поддержка

При возникновении проблем:
- Проверьте логи в папке `logs/`
- Проверьте структуру Excel файла
- Убедитесь, что БД доступна (`data/stock_analysis.db`)

## Часто задаваемые вопросы

**В: Потеряются ли мои старые данные?**  
О: Нет, все данные остаются в базе данных. Изменяется только структура Excel файла.

**В: Нужно ли переносить котировки из старого Excel в новый?**  
О: Нет, котировки теперь хранятся в БД. Excel нужен только для списка тикеров.

**В: Можно ли вернуться к старой версии?**  
О: Да, используя резервную копию Excel и откат git.

**В: Как обновить цену акции вручную?**  
О: Цены обновляются автоматически при каждом анализе. Ручное обновление не требуется.

**В: Что делать, если компания не удаляется?**  
О: Проверьте логи. Новая версия должна удалять все связанные данные автоматически.
