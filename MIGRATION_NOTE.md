# Примечание о миграции структуры проекта

## ✅ Проект реорганизован для Git

Дата: 2026-02-04

Структура проекта была реорганизована для соответствия лучшим практикам Git и упрощения навигации.

## 🔄 Основные изменения

### 1. Новые каталоги
- `bin/` - все .bat файлы для запуска
- `docs/en/` - английская документация
- `docs/ru/` - русская документация
- `scripts/` - утилитарные скрипты
- `data/samples/` - образцы данных

### 2. Перемещенные файлы
- `.bat` файлы → `bin/`
- Документация → `docs/ru/` и `docs/en/`
- Утилиты → `scripts/`
- `dashboards/` → `src/dashboards/`
- Примеры Excel → `data/samples/`

### 3. Обновления кода
- ✅ `app.py`: импорты dashboards обновлены
- ✅ Все `.bat` файлы: добавлен переход в корень проекта
- ✅ `.gitignore`: обновлен для новой структуры
- ✅ `README.md`: обновлена документация

## 🎯 Действия для существующих пользователей

### Если вы уже используете проект:

1. **Обновите конфигурацию:**
   - Ваш `config.yaml` остался на месте (в корне)
   - Никаких изменений не требуется

2. **Используйте новые пути запуска:**
   ```bash
   # Старый способ (больше не работает):
   start_web.bat
   
   # Новый способ:
   bin\start_web.bat
   ```

3. **Виртуальное окружение:**
   - Ваше `venv/` осталось на месте
   - Пересоздавать не нужно

4. **База данных и данные:**
   - Все данные в `data/` сохранены
   - Структура БД не изменилась

### Если начинаете с нуля:

1. Клонируйте репозиторий
2. Запустите `bin\setup.bat`
3. Настройте `config.yaml`
4. Запустите `bin\start_web.bat`

## 📖 Документация

- Полная структура: `PROJECT_STRUCTURE.md`
- Основная документация: `README.md`
- Инструкции по развертыванию: `docs/en/DEPLOYMENT.md`
- Русские инструкции: `docs/ru/`

## ⚠️ Важно для Git

Проект теперь готов к коммиту:
- ✅ Чувствительные данные игнорируются
- ✅ Структура папок сохраняется
- ✅ Примеры конфигурации включены
- ✅ Документация организована

## 🔍 Проверка

Убедитесь, что все работает:
```bash
# Проверка зависимостей
bin\check_dependencies.bat

# Запуск веб-интерфейса
bin\start_web.bat
```

## 📞 Поддержка

При возникновении проблем:
1. Проверьте `PROJECT_STRUCTURE.md`
2. Проверьте логи в `logs/`
3. Убедитесь, что используете скрипты из `bin/`

---

Все изменения обратно совместимы. Ваши данные и конфигурация сохранены.
