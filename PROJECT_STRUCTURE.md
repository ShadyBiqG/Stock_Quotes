# Структура проекта Stock Quotes Analyzer

## 📁 Организация файлов и папок

Проект реорганизован для удобной работы с Git и соответствия лучшим практикам.

### Основные каталоги

```
Stock_Quotes/
├── bin/                    # Исполняемые скрипты (.bat файлы для Windows)
├── docs/                   # Документация (ru/ и en/)
├── scripts/                # Утилиты и вспомогательные скрипты
├── src/                    # Исходный код приложения
├── data/                   # Данные и кэш
├── output/                 # Выходные файлы (экспорты)
├── logs/                   # Логи приложения
└── deploy/                 # Файлы для развертывания (Docker)
```

### Описание каталогов

#### `bin/` - Скрипты запуска
Все `.bat` файлы для запуска различных компонентов системы:
- `setup.bat` - установка зависимостей
- `start_web.bat` - запуск веб-интерфейса
- `start.bat` - запуск CLI версии
- `start_scheduler.bat` - запуск планировщика
- `quick_start.bat` - быстрый старт
- `check_dependencies.bat` - проверка зависимостей
- `clear_database.bat` - очистка базы данных

**Использование:**
```bash
# Запуск из корня проекта
bin\start_web.bat

# Или из папки bin
cd bin
start_web.bat
```

#### `docs/` - Документация
Вся документация разделена по языкам:
- `docs/en/` - английская документация (DEPLOYMENT.md, USER_GUIDE.md и т.д.)
- `docs/ru/` - русская документация (ЗАПУСК.txt, КАК_ОБНОВИТЬ_КОТИРОВКИ.txt и т.д.)

#### `scripts/` - Утилиты
Вспомогательные Python скрипты:
- `check_database.py` - проверка состояния БД
- `clear_database.py` - очистка данных
- `update_stock_prices.py` - обновление котировок
- `compare_tickers.py` - сравнение тикеров
- И другие утилитарные скрипты

**Использование:**
```bash
python scripts\check_database.py
python scripts\update_stock_prices.py
```

#### `src/` - Исходный код
Основной код приложения:
- `data_loader.py` - загрузка данных из Excel
- `llm_manager.py` - работа с OpenRouter API
- `analyzer.py` - логика анализа
- `database.py` - работа с SQLite
- `excel_exporter.py` - экспорт в Excel
- `company_info.py` - информация о компаниях
- `dashboards/` - модули веб-интерфейса Streamlit
  - `overview.py` - обзор
  - `analysis.py` - анализ
  - `history.py` - история
  - `accuracy.py` - точность
  - `settings.py` - настройки

**Импорты в коде:**
```python
from src.data_loader import DataLoader
from src.dashboards import overview, analysis
```

#### `data/` - Данные
Хранение данных приложения:
- `samples/` - образцы данных (Stock quotes.xlsx)
- `cache/` - кэш данных
- `*.db` - SQLite базы данных (игнорируются Git)

#### `output/` - Выходные файлы
- `exports/` - экспортированные Excel отчеты (игнорируются Git)

#### `logs/` - Логи
- Лог-файлы приложения (игнорируются Git)
- Сохраняется структура папки через `.gitkeep`

#### `deploy/` - Развертывание
- `docker-compose.yml` - Docker Compose конфигурация
- `.env.example` - пример переменных окружения

### Корневые файлы

- `main.py` - точка входа CLI приложения
- `app.py` - точка входа веб-приложения (Streamlit)
- `scheduler.py` - планировщик задач
- `config.yaml` - конфигурация (создать из config.example.yaml)
- `config.example.yaml` - пример конфигурации
- `requirements.txt` - Python зависимости
- `.gitignore` - игнорируемые файлы Git
- `Dockerfile` - образ Docker
- `README.md` - основная документация

## 🔧 Что изменилось

### Для пользователей
1. **Запуск приложения:** используйте скрипты из папки `bin/`
   ```bash
   bin\start_web.bat    # вместо start_web.bat
   ```

2. **Документация:** теперь в папке `docs/` (разделена по языкам)

3. **Примеры данных:** в папке `data/samples/`

### Для разработчиков
1. **Импорты dashboards:** обновлены на `from src.dashboards`
   ```python
   # Было:
   from dashboards import overview
   
   # Стало:
   from src.dashboards import overview
   ```

2. **Структура Git:** проект готов к коммиту:
   - Чувствительные данные игнорируются
   - Сохранена структура папок через `.gitkeep`
   - Обновлен `.gitignore`

## 📝 .gitignore

Игнорируются:
- `config.yaml` (содержит API ключи)
- `data/*.db` (базы данных)
- `data/cache/*` (кэш)
- `logs/*.log` (логи)
- `output/exports/*.xlsx` (экспорты)
- `venv/`, `__pycache__/` (окружение и кэш Python)

Сохраняются:
- `config.example.yaml` (пример конфигурации)
- `.gitkeep` файлы (структура папок)
- `data/samples/` (образцы данных)

## 🚀 Быстрый старт

1. Клонируйте репозиторий
2. Запустите `bin\setup.bat`
3. Скопируйте `config.example.yaml` → `config.yaml`
4. Настройте API ключ в `config.yaml`
5. Запустите `bin\start_web.bat`

## 📚 Дополнительно

- Основная документация: `README.md`
- Развертывание: `docs/en/DEPLOYMENT.md`
- Руководство пользователя: `docs/en/USER_GUIDE.md`
- Инструкции на русском: `docs/ru/`

---

**Обновлено:** 2026-02-04  
**Версия структуры:** 2.0
