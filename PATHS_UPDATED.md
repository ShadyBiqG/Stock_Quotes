# ✅ Пути к файлам обновлены

**Дата:** 2026-02-04  
**Причина:** Реструктуризация проекта

## 🔄 Что изменилось

После реструктуризации проекта файлы данных были перемещены в `data/samples/`, что требовало обновления путей в коде и конфигурации.

### Обновленные пути

#### Конфигурация (`config.yaml`)
Добавлен новый параметр для указания пути к файлу с котировками:

```yaml
# Путь к файлу с котировками
input:
  excel_file: "data/samples/Stock quotes.xlsx"
```

#### Обновленные файлы кода

1. **`main.py`**
   - Теперь читает путь из `config.yaml`
   - Поддержка пути по умолчанию `data/samples/Stock quotes.xlsx`

2. **`src/data_loader.py`**
   - Обновлен пример использования

3. **`src/analyzer.py`**
   - Путь по умолчанию: `data/samples/Stock quotes.xlsx`

4. **`src/dashboards/overview.py`**
   - Путь обновлен на `data/samples/Stock quotes.xlsx`

5. **`src/dashboards/settings.py`**
   - Загрузка файлов в `data/samples/`
   - Проверка файла по новому пути

## 📁 Где находятся файлы

### Образцы данных
```
data/samples/
├── Stock quotes.xlsx           # Основной файл с котировками
├── Stock quotes_backup.xlsx    # Резервная копия
└── Stock_quotes_with_data.xlsx # Файл с данными
```

### Конфигурация
- `config.yaml` - рабочая конфигурация (в корне)
- `config.example.yaml` - пример конфигурации (в корне)

### База данных
- `data/stock_analysis.db` - SQLite база данных

### Экспорты
- `output/exports/*.xlsx` - экспортированные отчеты

## 🚀 Использование

### Вариант 1: Использовать настройки по умолчанию

Если в вашем `config.yaml` указано:
```yaml
input:
  excel_file: "data/samples/Stock quotes.xlsx"
```

То приложение автоматически будет использовать файл из `data/samples/`.

### Вариант 2: Указать свой путь

Вы можете использовать любой путь:
```yaml
input:
  excel_file: "my_data/my_stocks.xlsx"
```

### Вариант 3: Загрузка через веб-интерфейс

В веб-приложении (страница **Настройки → Анализ**):
1. Нажмите "📤 Загрузить новый Excel файл"
2. Выберите файл
3. Файл автоматически сохранится в `data/samples/Stock quotes.xlsx`

## ⚠️ Важно

### Для существующих пользователей

Если вы обновляете проект и у вас есть файл `Stock quotes.xlsx` в корне:

**Вариант 1: Переместить файл**
```bash
# Windows
move "Stock quotes.xlsx" "data\samples\"

# Linux/Mac
mv "Stock quotes.xlsx" "data/samples/"
```

**Вариант 2: Обновить config.yaml**
```yaml
input:
  excel_file: "Stock quotes.xlsx"  # Оставить путь к корню
```

### Проверка конфигурации

Убедитесь, что файл существует:
```bash
# Windows
dir "data\samples\Stock quotes.xlsx"

# Linux/Mac
ls -la "data/samples/Stock quotes.xlsx"
```

## 📝 Примеры использования

### CLI запуск
```bash
# Запуск с файлом по умолчанию (из config.yaml)
bin\start.bat

# Или напрямую
python main.py
```

### Веб-интерфейс
```bash
# Запуск веб-приложения
bin\start_web.bat

# Откройте http://localhost:8501
# Перейдите в "Настройки" для управления файлами
```

## 🔍 Диагностика проблем

### Ошибка: "Файл Stock quotes.xlsx не найден"

**Причина:** Файл не найден по указанному пути.

**Решение:**
1. Проверьте `config.yaml`:
   ```yaml
   input:
     excel_file: "data/samples/Stock quotes.xlsx"
   ```

2. Проверьте наличие файла:
   ```bash
   dir data\samples\Stock quotes.xlsx
   ```

3. Если файла нет, скопируйте из образцов или создайте свой

### Ошибка: "Недостаточно прав доступа"

**Решение:**
- Убедитесь, что папка `data/samples/` существует
- Проверьте права доступа к папке

## 📚 Дополнительная информация

- **Структура проекта:** `PROJECT_STRUCTURE.md`
- **Миграция:** `MIGRATION_NOTE.md`
- **Основная документация:** `README.md`

---

**Все пути обновлены и работают! ✅**
