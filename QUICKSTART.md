# 🚀 Быстрый старт

Руководство по запуску Stock Quotes Analyzer за 5 минут.

## 📋 Предварительные требования

- Python 3.10+
- OpenRouter API ключ ([получить здесь](https://openrouter.ai/))
- Excel файл с котировками

## ⚡ Установка

### 1. Клонирование/распаковка проекта

```bash
cd Stock_Quotes
```

### 2. Создание виртуального окружения (опционально, но рекомендуется)

**Windows:**
```powershell
python -m venv venv
.\venv\Scripts\activate
```

**Linux/Mac:**
```bash
python3 -m venv venv
source venv/bin/activate
```

### 3. Установка зависимостей

```bash
pip install -r requirements.txt
```

## 🔑 Настройка API ключа

### Вариант 1: Через config.yaml

Откройте `config.yaml` и замените:

```yaml
openrouter:
  api_key: "your-openrouter-api-key-here"  # <-- Ваш ключ
```

### Вариант 2: Через переменную окружения

**Windows:**
```powershell
$env:OPENROUTER_API_KEY = "ваш-ключ"
```

**Linux/Mac:**
```bash
export OPENROUTER_API_KEY="ваш-ключ"
```

## 📊 Подготовка данных

Убедитесь, что файл `Stock quotes.xlsx` содержит колонки:

| Ticker | Price | Change | Volume | Info (опционально) |
|--------|-------|--------|--------|-------------------|
| AAPL   | 150.25| 2.5    | 1000000| ...               |
| GOOGL  | 2800.5| -1.2   | 500000 | ...               |

## 🎯 Запуск

### Вариант 1: CLI (командная строка)

```bash
python main.py
```

**Что произойдет:**
- ✅ Загрузка данных из Excel
- ✅ Запрос к 3 LLM моделям для каждой акции
- ✅ Сохранение в базу данных SQLite
- ✅ Создание Excel отчета в `output/exports/`

**Пример вывода:**
```
╔═══════════════════════════════════════════════════════════════╗
║           📊 Stock Quotes Analyzer 📊                        ║
╚═══════════════════════════════════════════════════════════════╝

🔧 Загрузка конфигурации...
📂 Загрузка данных из Stock quotes.xlsx...
✅ Загружено 50 акций

🚀 Запуск анализа 50 акций...
   Модели: GPT-4 Turbo, Claude 3 Sonnet, Gemini Pro

Анализ акций: 100%|██████████████████| 50/50 [02:15<00:00,  2.71s/it]

============================================================
📊 РЕЗУЛЬТАТЫ АНАЛИЗА
============================================================

⏱️  Время выполнения: 135.3 секунд

✅ Успешно проанализировано: 50 акций
❌ Ошибок: 0 акций

📈 Прогнозы:
   🟢 Растут:     28 ( 56.0%)
   🔴 Падают:     15 ( 30.0%)
   🟡 Стабильны:   7 ( 14.0%)

🤝 Консенсус моделей: 84.0%

============================================================

📄 Экспорт результатов в Excel...
✅ Отчет сохранен: output\exports\2026-02-04_analysis.xlsx

✨ Анализ завершен успешно!
```

### Вариант 2: Веб-приложение

```bash
streamlit run app.py
```

**Что произойдет:**
- 🌐 Откроется браузер на http://localhost:8501
- 📱 Адаптивный интерфейс (работает на телефоне)
- 📊 Интерактивные дашборды

**Доступные страницы:**
- 🏠 **Обзор** - метрики и последние результаты
- 📈 **Анализ** - детальный просмотр с причинами
- 📜 **История** - тренды во времени
- 🎯 **Точность** - статистика моделей
- ⚙️ **Настройки** - запуск анализа, экспорт

### Вариант 3: Автоматический режим (планировщик)

Настройте расписание в `config.yaml`:

```yaml
scheduler:
  enabled: true  # Включить
  schedule:
    - cron: "0 19 * * 1-5"  # Будни в 19:00
```

Запустите:

```bash
python scheduler.py
```

## 🐳 Docker (опционально)

### Быстрый запуск

```bash
# 1. Создайте .env в папке deploy/
cd deploy
cp .env.example .env
# Отредактируйте .env и добавьте API ключ

# 2. Запустите
docker-compose up -d

# Приложение доступно на http://localhost:8501
```

## 📱 Доступ с телефона

### В локальной сети:

1. Узнайте IP вашего компьютера:
   ```bash
   # Windows
   ipconfig
   
   # Linux/Mac
   hostname -I
   ```

2. На телефоне откройте:
   ```
   http://192.168.x.x:8501
   ```

### Через интернет:

Для быстрого теста используйте ngrok:

```bash
# Установите ngrok
# https://ngrok.com/download

# Запустите туннель
ngrok http 8501

# Получите публичный URL
# https://abc123.ngrok.io
```

## 🎨 Примеры использования

### Пример 1: Быстрый анализ

```bash
# Положите Stock quotes.xlsx в папку
# Запустите
python main.py

# Получите Excel отчет
# output/exports/2026-02-04_analysis.xlsx
```

### Пример 2: Просмотр результатов

```bash
# Запустите веб-приложение
streamlit run app.py

# Откройте 🏠 Обзор
# Посмотрите метрики и топ акций
```

### Пример 3: История трендов

```bash
# Запустите несколько анализов за разные дни
python main.py

# В веб-приложении откройте 📜 История
# Выберите акцию и посмотрите тренды
```

## ❓ Частые вопросы

### Ошибка "API ключ не настроен"

**Решение:** Добавьте ключ в `config.yaml` или переменную окружения `OPENROUTER_API_KEY`

### Ошибка "Файл Stock quotes.xlsx не найден"

**Решение:** Убедитесь, что файл находится в корне проекта рядом с `main.py`

### Ошибка "ModuleNotFoundError"

**Решение:** Установите зависимости: `pip install -r requirements.txt`

### Медленная работа

**Решение:** 
- Уменьшите количество акций
- Используйте меньше моделей
- Увеличьте max_tokens в config.yaml до меньшего значения

### Ошибки валидации LLM

**Решение:** Это нормально! Некоторые ответы могут быть отклонены. Система автоматически повторит попытку.

## 💰 Примерная стоимость

| Акций | Моделей | Токенов | Стоимость |
|-------|---------|---------|-----------|
| 10    | 3       | ~25,000 | ~$0.20    |
| 50    | 3       | ~125,000| ~$1.00    |
| 100   | 3       | ~250,000| ~$2.00    |

**Совет:** Начните с небольшого набора акций для тестирования!

## 📚 Дополнительно

- [README.md](README.md) - Полная документация
- [config.yaml](config.yaml) - Настройки
- Логи: `logs/analysis.log`
- База данных: `data/stock_analysis.db`
- Отчеты: `output/exports/`

## 🆘 Поддержка

Если возникли проблемы:
1. Проверьте логи в `logs/analysis.log`
2. Убедитесь, что API ключ корректный
3. Проверьте формат Excel файла
4. Откройте Issue на GitHub

## 🎉 Готово!

Теперь вы можете анализировать котировки акций с помощью AI! 🚀
