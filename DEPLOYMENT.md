# 🚀 Руководство по развертыванию на новом сервере

## 📋 Требования

### Системные требования:
- **ОС:** Windows 10/11 или Windows Server 2016+
- **Python:** 3.10 или новее
- **RAM:** Минимум 2 GB, рекомендуется 4 GB
- **Диск:** Минимум 500 MB свободного места
- **Интернет:** Для доступа к OpenRouter API

### Программное обеспечение:
- Python 3.10+ ([скачать](https://www.python.org/downloads/))
- Git (опционально, для клонирования репозитория)

---

## 📥 Шаг 1: Получение файлов проекта

### Вариант A: Из Git репозитория
```bash
git clone <URL-репозитория>
cd Stock_Quotes
```

### Вариант B: Копирование архива
1. Распакуйте архив в целевую папку
2. Откройте командную строку в папке проекта

---

## ⚙️ Шаг 2: Настройка конфигурации

### 1. Создайте файл config.yaml
```bash
copy config.example.yaml config.yaml
```

### 2. Получите API ключ OpenRouter
1. Перейдите на [https://openrouter.ai/](https://openrouter.ai/)
2. Зарегистрируйтесь или войдите
3. Перейдите в раздел **API Keys**
4. Создайте новый ключ
5. Скопируйте ключ

### 3. Настройте config.yaml
Откройте `config.yaml` в текстовом редакторе и замените:

```yaml
openrouter:
  api_key: "your-openrouter-api-key-here"  # ← Вставьте ваш ключ
```

на:

```yaml
openrouter:
  api_key: "sk-or-v1-ваш-реальный-ключ-здесь"
```

### 4. (Опционально) Настройте Alphavantage API
Для экономии токенов OpenRouter:

1. Получите бесплатный ключ: [https://www.alphavantage.co/support/#api-key](https://www.alphavantage.co/support/#api-key)
2. В `config.yaml`:
   ```yaml
   company_info:
     alphavantage_api_key: "ваш-ключ-alphavantage"
   ```

---

## 🔧 Шаг 3: Установка зависимостей

### Автоматическая установка (рекомендуется)
Просто запустите:
```bash
setup.bat
```

Скрипт автоматически:
- ✅ Проверит Python
- ✅ Создаст виртуальное окружение
- ✅ Установит все зависимости
- ✅ Проверит конфигурацию
- ✅ Создаст необходимые папки

### Ручная установка (если setup.bat не работает)
```bash
# 1. Создайте виртуальное окружение
python -m venv venv

# 2. Активируйте его
venv\Scripts\activate.bat

# 3. Обновите pip
pip install --upgrade pip

# 4. Установите зависимости
pip install -r requirements.txt

# 5. Создайте папки
mkdir data\cache
mkdir output\exports
mkdir logs
```

---

## 📊 Шаг 4: Подготовка данных

### Создайте файл Stock quotes.xlsx

Формат файла Excel:

| Ticker | Price  | Change | Volume    | Info (опционально)      |
|--------|--------|--------|-----------|-------------------------|
| AAPL   | 185.50 | 2.5    | 50000000  | Apple Inc.              |
| GOOGL  | 142.30 | -1.2   | 25000000  | Alphabet Inc.           |
| MSFT   | 380.00 | 0.8    | 30000000  | Microsoft Corporation   |

**Требования:**
- Обязательные колонки: `Ticker`, `Price`, `Change`, `Volume`
- Колонка `Info` опциональна (будет получена из API/LLM)

### Или используйте тестовый файл
В репозитории уже есть файл `Stock quotes.xlsx` с 10 тестовыми акциями.

---

## ✅ Шаг 5: Проверка установки

Запустите диагностику:
```bash
check_dependencies.bat
```

Скрипт проверит:
- ✅ Python и pip
- ✅ Виртуальное окружение
- ✅ Все зависимости
- ✅ Структуру проекта
- ✅ Конфигурацию API ключа

---

## 🚀 Шаг 6: Первый запуск

### Вариант 1: CLI анализ
```bash
start.bat
```
Результаты в `output\exports\ГГГГ-ММ-ДД_analysis.xlsx`

### Вариант 2: Веб-интерфейс (рекомендуется)
```bash
start_web.bat
```
Откроется браузер с адресом `http://localhost:8501`

### Вариант 3: Автоматический планировщик
1. В `config.yaml` включите планировщик:
   ```yaml
   scheduler:
     enabled: true
   ```
2. Настройте расписание (cron формат)
3. Запустите:
   ```bash
   start_scheduler.bat
   ```

---

## 🔒 Безопасность

### Защита API ключа

#### Вариант 1: Переменная окружения (рекомендуется)
```bash
# Временная установка (до перезагрузки)
set OPENROUTER_API_KEY=ваш-ключ

# Постоянная установка
setx OPENROUTER_API_KEY "ваш-ключ"
```

В `config.yaml` оставьте пустым:
```yaml
openrouter:
  api_key: ""  # Будет взят из переменной окружения
```

#### Вариант 2: .env файл
1. Создайте файл `.env`:
   ```
   OPENROUTER_API_KEY=ваш-ключ
   ```
2. Добавьте `.env` в `.gitignore`

### Важно для Git
Если используете Git, убедитесь что `config.yaml` в `.gitignore`:
```
config.yaml
.env
data/*.db
logs/*.log
```

---

## 🌐 Развертывание на удаленном сервере

### Windows Server

1. **Установите Python:**
   - Скачайте MSI установщик
   - Отметьте "Add Python to PATH"
   - Установите

2. **Скопируйте проект:**
   ```bash
   xcopy /E /I \\source\Stock_Quotes C:\Projects\Stock_Quotes
   cd C:\Projects\Stock_Quotes
   ```

3. **Выполните Шаги 2-5** из этой инструкции

4. **Настройте брандмауэр (для веб-интерфейса):**
   ```bash
   netsh advfirewall firewall add rule name="Stock Analyzer Web" dir=in action=allow protocol=TCP localport=8501
   ```

5. **Создайте задачу в Task Scheduler:**
   - Действие: Запуск программы
   - Программа: `C:\Projects\Stock_Quotes\start.bat`
   - Расписание: По вашему усмотрению

### Linux (через Docker)

1. **Установите Docker:**
   ```bash
   curl -fsSL https://get.docker.com -o get-docker.sh
   sudo sh get-docker.sh
   ```

2. **Соберите образ:**
   ```bash
   docker build -t stock-analyzer .
   ```

3. **Запустите контейнер:**
   ```bash
   docker-compose up -d
   ```

---

## 📊 Мониторинг

### Логи
Все логи сохраняются в `logs\analysis.log`

Для просмотра в реальном времени:
```bash
Get-Content logs\analysis.log -Wait -Tail 20
```

### База данных
SQLite база: `data\stock_analysis.db`

Просмотр через SQLite Browser:
```bash
# Установите: https://sqlitebrowser.org/
# Откройте файл data\stock_analysis.db
```

---

## 🐛 Устранение проблем

### Проблема 1: "Python не найден"
**Решение:**
1. Установите Python с [python.org](https://www.python.org/)
2. При установке отметьте "Add Python to PATH"
3. Перезагрузите командную строку

### Проблема 2: "Ошибка установки зависимостей"
**Решение:**
```bash
# Обновите pip
python -m pip install --upgrade pip

# Переустановите зависимости
pip install -r requirements.txt --force-reinstall
```

### Проблема 3: "API ключ не работает"
**Решение:**
1. Проверьте ключ на [openrouter.ai/keys](https://openrouter.ai/keys)
2. Убедитесь, что на балансе есть средства
3. Проверьте формат в config.yaml (без пробелов, в кавычках)

### Проблема 4: "Yahoo Finance не работает"
**Это нормально!** LLM fallback автоматически подхватит:
- ✅ Система попробует Alphavantage (если настроен)
- ✅ Затем Yahoo Finance через HTTP
- ✅ Если не сработало → LLM (всегда работает)

### Проблема 5: "UnicodeEncodeError в консоли"
**Решение:**
```bash
# В начале BAT-файла добавлено:
chcp 65001
```
Уже исправлено в `main.py` для Windows.

---

## 📚 Дополнительные материалы

- [README.md](README.md) - Полная документация проекта
- [COMPANY_INFO_GUIDE.md](COMPANY_INFO_GUIDE.md) - Руководство по источникам данных
- [OPENROUTER_MODELS.md](OPENROUTER_MODELS.md) - Актуальные модели OpenRouter
- [CHANGELOG.md](CHANGELOG.md) - История изменений

---

## 💰 Стоимость работы

### Примерные расходы на OpenRouter API:

| Объем | LLM только | С Alphavantage |
|-------|-----------|----------------|
| 10 акций | ~$0.35-0.45 | ~$0.02 |
| 50 акций | ~$1.50-2.00 | ~$0.10 |
| 100 акций | ~$3.50-4.50 | ~$0.50 |

*Цены актуальны на февраль 2026*

### Как снизить расходы:
1. **Используйте Alphavantage API** (бесплатно, 500 req/день)
2. **Уменьшите количество моделей** (2 вместо 3)
3. **Используйте более дешевые модели** (Gemini Flash вместо GPT-4)

---

## ✅ Контрольный список развертывания

- [ ] Python 3.10+ установлен
- [ ] Проект скопирован на сервер
- [ ] config.yaml создан и настроен
- [ ] OpenRouter API ключ получен и добавлен
- [ ] setup.bat выполнен успешно
- [ ] check_dependencies.bat показывает ✅
- [ ] Stock quotes.xlsx создан с данными
- [ ] Первый анализ выполнен (start.bat)
- [ ] Excel отчет создан в output/exports/
- [ ] Веб-интерфейс запускается (start_web.bat)
- [ ] (Опционально) Планировщик настроен
- [ ] (Опционально) Alphavantage API настроен
- [ ] Логи пишутся в logs/analysis.log
- [ ] База данных создана в data/stock_analysis.db

---

## 🆘 Поддержка

Если возникли проблемы:

1. **Проверьте логи:** `logs\analysis.log`
2. **Запустите диагностику:** `check_dependencies.bat`
3. **Проверьте документацию:** [README.md](README.md)
4. **Создайте Issue** в репозитории проекта

---

**Версия:** 1.0.0  
**Дата:** 2026-02-04  
**Статус:** ✅ Готово к production использованию
