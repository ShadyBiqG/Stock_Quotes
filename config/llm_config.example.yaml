# ============================================================================
# КОНФИГУРАЦИЯ LLM И ОБЩИЕ НАСТРОЙКИ ПРОЕКТА (v3.0)
# ============================================================================
#
# API ключи хранятся отдельно в: config/api_keys.yaml
# Список компаний в: config/companies.json
#
# Скопируйте этот файл в config/llm_config.yaml и настройте под себя
#
# ============================================================================

# Входные данные
input:
  excel_file: "config/companies.json"  # v3.0: используем JSON вместо Excel

# OpenRouter базовый URL
openrouter:
  base_url: "https://openrouter.ai/api/v1"

# Модели для анализа
models:
  - name: "GPT-4 Turbo"
    id: "openai/gpt-4-turbo"
    temperature: 0.3  # Низкая для уменьшения галлюцинаций
    max_tokens: 1500  # Увеличено для развернутых ответов
    
  - name: "Claude 3.5 Sonnet"
    id: "anthropic/claude-3.5-sonnet"
    temperature: 0.3
    max_tokens: 1500
    
  - name: "Gemini Flash 1.5"
    id: "google/gemini-flash-1.5"
    temperature: 0.3
    max_tokens: 1500

# System prompt - защита от галлюцинаций
system_prompt: |
  Ты - профессиональный финансовый аналитик с 15-летним опытом работы на фондовом рынке.
  Твоя специализация - анализ технических показателей и движения цен акций.
  
  ТВОЯ ЗАДАЧА:
  - Давать РАЗВЕРНУТЫЕ и ИНФОРМАТИВНЫЕ оценки для инвесторов
  - Объяснять причины движения цены простым и понятным языком
  - Указывать конкретные уровни и цифры из данных
  - Давать контекст и интерпретацию технических показателей
  
  КРИТИЧЕСКИ ВАЖНО:
  - ВСЕГДА отвечай ТОЛЬКО на РУССКОМ языке
  - Используй ТОЛЬКО данные из запроса (не придумывай новости)
  - Будь конкретным: называй точные цифры, проценты, уровни
  - Объясняй ЧТО означают данные показатели для инвестора
  - Если данных недостаточно - честно укажи это
  - Отвечай строго в указанном формате
  - Все тексты, причины и объяснения должны быть на русском языке

# Промпт для анализа - развернутые ответы
prompt_template: |
  Проанализируй движение акции {ticker} как профессиональный аналитик для частного инвестора.
  
  📊 ТЕХНИЧЕСКИЕ ДАННЫЕ:
  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  Тикер: {ticker}
  Текущая цена: ${price} USD
  Изменение за период: {change}%
  Торговый объем: {volume:,} акций
  Доп. информация: {additional_info}
  
  📋 ТВОЯ ЗАДАЧА:
  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  1. Определи тренд: РАСТЕТ / ПАДАЕТ / СТАБИЛЬНА (±0.5%)
  2. Дай РАЗВЕРНУТЫЙ анализ с конкретными цифрами (4-6 предложений):
     • ЧТО происходит с ценой и объемом
     • ПОЧЕМУ это происходит (технический анализ данных)
     • ЧТО это означает для инвестора
     • Какие уровни/показатели важны
  
  ⚠️ СТРОГИЕ ПРАВИЛА:
  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  ✓ ОТВЕЧАЙ ТОЛЬКО НА РУССКОМ ЯЗЫКЕ
  ✓ Используй КОНКРЕТНЫЕ ЦИФРЫ из данных (цена, %, объем)
  ✓ Объясняй значение показателей простым языком
  ✓ НЕ придумывай новости или внешние события
  ✓ Если данных мало - опирайся на технические индикаторы
  
  📝 ФОРМАТ ОТВЕТА:
  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  ПРОГНОЗ: [РАСТЕТ/ПАДАЕТ/СТАБИЛЬНА]
  
  АНАЛИЗ:
  [Напиши 4-6 развернутых предложений с конкретными цифрами и их интерпретацией.
  Объясни динамику цены, что показывает объем торгов, какие технические уровни
  важны, и что это означает для инвестора. Используй данные: цена, изменение %,
  объем. Будь конкретным и информативным!]
  
  КЛЮЧЕВЫЕ ФАКТОРЫ:
  • [Фактор 1 с конкретными цифрами из данных]
  • [Фактор 2 с конкретными цифрами из данных]
  • [Фактор 3 с техническими показателями или их интерпретацией]
  
  УВЕРЕННОСТЬ: [ВЫСОКАЯ/СРЕДНЯЯ/НИЗКАЯ]
  
  ВАЖНО: Пиши развернуто и понятно для частного инвестора!

# База данных
database:
  path: "data/stocks.db"  # Используем одну БД для всех данных
  keep_history: true  # Сохранять данные по датам

# Настройки обновления котировок (v3.0)
price_updates:
  # Стратегия обновления:
  # - "always": всегда загружать свежие котировки через API (медленно, но актуально)
  # - "daily": обновлять раз в день (быстро для повторных запусков в течение дня)
  # - "cache_only": использовать только кэш из БД (самое быстрое, но данные могут устареть)
  strategy: "daily"
  
  # Минимальный интервал между обновлениями (в минутах)
  # Используется при strategy: "always"
  min_update_interval_minutes: 15
  
# Экспорт
export:
  output_path: "output/exports/{date}_analysis.xlsx"
  color_coding: true

# Настройки получения описания компаний
company_info:
  # Источники данных (в порядке приоритета):
  # 1. Alphavantage API (если указан ключ)
  # 2. Yahoo Finance через прямые HTTP запросы
  # 3. LLM fallback (всегда работает)
  
  fallback_to_llm: true         # Использовать LLM если API не дали результат
  cache_duration_days: 30       # Кэш на 30 дней
  llm_model: "openai/gpt-3.5-turbo"  # Дешевая модель для описаний

# ============================================================================
# ПЛАНИРОВЩИК АВТОМАТИЧЕСКОГО АНАЛИЗА
# ============================================================================
# Позволяет автоматически запускать анализ котировок по расписанию.
#
# При каждом срабатывании:
#   1. Загружаются компании из config/companies.json
#   2. Обновляются котировки через Yahoo Finance
#   3. Запускается анализ через все настроенные LLM-модели
#   4. Результаты сохраняются в базу данных
#   5. Генерируется Excel-отчёт в output/exports/
#   6. Все действия логируются в logs/scheduler.log
#
# Запуск планировщика:
#   Windows: bin\start_scheduler.bat
#   Linux:   python scheduler.py
# ============================================================================
scheduler:
  # Включить/выключить планировщик
  # true  = планировщик активен и будет запускать анализ по расписанию
  # false = планировщик отключён (по умолчанию)
  enabled: false
  
  # Временная зона для расписания
  # Список зон: https://en.wikipedia.org/wiki/List_of_tz_database_time_zones
  # Примеры: Europe/Moscow, America/New_York, Asia/Tokyo, UTC
  timezone: "Europe/Moscow"
  
  # Расписание задач (можно указать несколько)
  # ──────────────────────────────────────────────────────────────────────────
  # Формат cron: минута час день месяц день_недели
  #
  # ┌───────────── минута (0-59)
  # │ ┌─────────── час (0-23)
  # │ │ ┌───────── день месяца (1-31)
  # │ │ │ ┌─────── месяц (1-12)
  # │ │ │ │ ┌───── день недели (0-6, где 0=Вс, 1=Пн, ..., 6=Сб)
  # │ │ │ │ │      Также можно: 1-5 означает Пн-Пт (будни)
  # * * * * *
  #
  # ПРИМЕРЫ:
  #   "0 19 * * 1-5"       = Каждый будний день (Пн-Пт) в 19:00
  #   "30 9 * * *"         = Каждый день в 9:30
  #   "0 */4 * * *"        = Каждые 4 часа (0:00, 4:00, 8:00, 12:00, 16:00, 20:00)
  #   "0 10,14,18 * * 1-5" = В 10:00, 14:00 и 18:00 по будням
  #   "0 9 * * 1"          = Каждый понедельник в 9:00
  #   "0 0 1 * *"          = Первого числа каждого месяца в полночь
  #   "0 22 * * 0"         = Каждое воскресенье в 22:00
  # ──────────────────────────────────────────────────────────────────────────
  schedule:
    - cron: "0 19 * * 1-5"
      description: "Ежедневный анализ после закрытия биржи"
    
    # Пример добавления второй задачи (раскомментируйте при необходимости):
    # - cron: "0 10 * * 1-5"
    #   description: "Утренний анализ перед открытием NYSE"
  
# Настройки веб-приложения
web_app:
  host: "0.0.0.0"  # Доступ с любого IP
  port: 8501       # Порт Streamlit
  theme: "dark"    # Темная тема
  mobile_friendly: true

# Настройки логирования
logging:
  level: "INFO"  # DEBUG, INFO, WARNING, ERROR
  format: "%(asctime)s - %(name)s - %(levelname)s - %(message)s"
